agents:
  greeter:
    prompt_key: greeter
    tools:
      - now
      - generate_image
      - post_to_x_with_image
    requires_approval: false   # set to true if you want manual approval before posting
